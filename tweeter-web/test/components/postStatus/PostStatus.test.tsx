import PostStatus from "../../../src/components/postStatus/PostStatus";

describe("PostStatus Component", () => {
  it("Start with post and clear buttons disabled", () => {});
  it("Enable both buttons when text field has text", () => {});
  it("Disables both buttons if text field is cleared", () => {});
  it("Calls presenter post method with correct parameters when post button is pressed", () => {});
});
